.DS_Store
.claude/settings.local.json
# This repo is the compass plugin, not a compass-managed project. Keep
# project-side artifacts out of the plugin so they never ship to users:
.work/
.mcp.json
.worktree-port
# Transient selftest reports (scripts/selftest.sh --report) — never committed:
reports/
